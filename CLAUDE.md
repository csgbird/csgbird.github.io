# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

Project name: **blogweb**. This project is currently uninitialized. The directory contains only IDE configuration (.idea/) and Claude Code settings (.claude/). No framework, source code, or content has been added yet.

## Claude Code Permissions

The `.claude/settings.json` grants full permissions for this project:
- All Bash commands (`Bash(*)`)
- All file read/write/edit operations
- Default mode: `acceptEdits`
